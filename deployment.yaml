apiVersion: apps/v1
kind: Deployment

metadata:
  name: my-dep

spec:
  replicas: 1

  selector:
    matchLabels:
      app: demo-app

  template:
    metadata:
      labels:
        app: demo-app

    spec:
      containers:
      - name: yashmane108/simple-pod:REPLACE

        image: simple-web:1

        ports:
        - containerPort: 80
